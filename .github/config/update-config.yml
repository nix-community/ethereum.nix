# Configuration for automatic updates
# See .github/actions/discovery.py for usage

skip:
  # Flake inputs to skip from auto-updates
  # Packages use passthru.skipAutoUpdate = true instead
  inputs: []
    # - nixpkgs  # example: pinned for stability
